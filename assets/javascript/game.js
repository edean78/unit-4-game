// Ready the document
$(document).ready(function () {
    console.log("Ready");
});

// Create Variables
// ===========================================================================================

var totalScore: 0;
var targerScore: 0;
var wins = 0;
var losses = 0;

var crystal = {
    color:
    randomNum:
}

// Create Function and Logic
// ============================================================================================

function initGame() {
    var minNumber = 19;
    var maxNumber = 120;

    targetScore = Math.floor(Math.random)
}